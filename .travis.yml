language: cpp

compiler:
  - gcc
  - clang

before_script:
  - export CFLAGS="-m64 -static -fPIC" && export CXXFLAGS="-m64 -static -fPIC" && export LDFLAGS="-m64 -static -fPIC"
  - mkdir _build
  - cd _build
  - cmake .. -DFORCE_OPERATIONS=TRUE -DUSE_BOOST_THREAD=TRUE 

script: cd _build && make 
