extraction:
  cpp:
    configure:
      command:
        - mkdir _build
        - cd _build
        - cmake .. -DFORCE_OPERATIONS=TRUE
    index:
        build_command:
            - cd _build
            - make